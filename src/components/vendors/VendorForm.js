// This module will display a new vendor form

import React, {useState, useEffect } from "react";
import { useHistory } from "react-router-dom/";

export const VendorForm = () => {
    return (
        <>{
            <p key={1}>This is the vendor form page, it will display the add vendor form.</p>
        }</>
    )
}